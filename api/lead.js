import nodemailer from "nodemailer"; export default async function handler(req, res) { if (req.method !== "POST") { return res.status(405).send("Method Not Allowed"); } const { name, email, website, message } = req.body; const transporter = nodemailer.createTransport({ service: "gmail", auth: { user: process.env.EMAIL_USER, pass: process.env.EMAIL_PASS } }); await transporter.sendMail({ from: process.env.EMAIL_USER, to: "time2devai@gmail.com", subject: `New FunnelLab Lead: ${name}`, text: ` Name: ${name} Email: ${email} Website: ${website} Message: ${message} ` }); res.status(200).json({ success: true }); }